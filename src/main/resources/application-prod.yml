server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mysql-1
    username: testuser
    password: testpassword
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: mysql